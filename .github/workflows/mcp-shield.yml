name: MCP Shield

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: thuggeelya/mcp-shield-action@v1
        with:
          server: 'npx -y @kimsungwhee/apple-docs-mcp'
